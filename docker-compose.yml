version: '3.8'
services:
  autoclaw:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./workspace:/app/workspace
      - ./backend:/app/backend
    restart: unless-stopped
    environment:
      - TZ=Asia/Shanghai
